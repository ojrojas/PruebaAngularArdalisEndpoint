export enum HorizontalPosition {
    start = 'start',
    center = 'center',
    end = 'end',
    left = 'left',
    right = 'right'
}

export enum VerticalPosition {
    top= 'top',
    bottom= 'bottom'
}
